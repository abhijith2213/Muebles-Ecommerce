
<html
  lang="en"
  class="light-style layout-menu-fixed"
  dir="ltr"
  data-theme="theme-default"
  data-template="vertical-menu-template-free">

<div class="layout-page m-3 ">
   <div class="table-top" style="margin-top: 30px; width: -webkit-fill-available;">
       <div class="card">  
        <div class="card-body">  
            <div class="d-flex p-1"><p >Customer Id: <span>{{order.userId}} </span></p></div>
        <div class="d-flex justify-content-between">
            <div class="align-items-center mt-5 ml-4">
                <div class="d-flex  rounded  p-1" id="border"><h5 class="m-0 pt-1" >Order ID:</h5> <span class="ml-2  p-1  ">{{order._id}}</span></div>
                <div class="d-flex p-1"><p >Ordered date : <span>{{order.date}}</span></p></div>
            </div>

             <form action="/admin/viewOrders/edit/{{order._id}}" method="post">
            <div class="align-items-center mt-5 mr-4" >
                <div class="fw-bolder">Current Status: <select name="orderStatus" id="orderStatus">
                    <option hidden selected value="{{order.orderStatus}}">{{order.orderStatus}}</option>                   
                    <option value="Shipped">shipped</option>
                    <option value="Delivered">delivered</option>
                    <option value="Cancelled">cancelled</option>                    
                </select></div>
                <div class="fw-bolder">Delivery Date: <span class="fw-normal">.....</span></div>
            </div>
            
           
        </div>
        <h6 class="p-3">Product Details:</h6>
            <table class="table mb-4">
                <tbody>
                    {{#each orderData}}       
                     <tr class="ml-3">
                        <td class="product-thumbnail" style="width: 170px;"><a href="" class="ml-4"><img src="/imageUploads/{{this.product.images.[0]}}" alt="image " style="width:80px ;"></a></td>
                        <td class="product-name">
                            <p><a href="" class="text-black">{{this.product.Product}}</a></p>
                        </td>
                        <td class="product-price "><span>₹ {{this.product.price}}</span> 
                        <p style="margin-right: 30px;">Qty: <span>{{this.quantity}}</span></p>
                        </td>
                                               
                        <td class="text-right " ><span style="font-size: 22px; margin-right: 30px;">{{../order.totalAmount}}</span>
                        
                        </td>
                                               
                    </tr>
                    {{/each}}
                     
                       
                </tbody>
            </table>

            <div class="d-flex justify-content-between">
                 <div class="align-items-center mt-2 mr-5">
                    <h6>Delivery Address</h6>
                    <p>{{order.deliveryDetails.firstName}}</p>
                    <p>{{order.deliveryDetails.phone}}</p>
                    <p>{{order.deliveryDetails.email}}</p>
                    <span>{{order.deliveryDetails.city}}</span>,<span>{{order.deliveryDetails.state}}</span>
                    <p>{{order.deliveryDetails.zip}}</p>
                </div>

                <div class="align-items-center mt-2 ml-4">
                    <h6 class="mb-0">Payment Method </h6>
                    <span>{{order.paymentMethod}}</span>
                    <h6 class="mt-2 mb-0">Payment Status</h6>
                    <span class="rounded-pill badge bg-success">{{order.orderStatus}}</span>
                </div>
               
                
                
               
            </div>
           
            <div class="mr-2">
                     {{!-- <a class="btn btn-primary text-white" style="background-color: #dd4b39; border-radius: 10px; float: right;" type="submit" >UPDATE</a> --}}
                      <button type="submit" class="btn btn-primary btn-block mb-4">Update Status</button>
                </div>
         </form>
        </div>
        </div>

            <style>
                p{
                    margin: 0;
                }

                #border{
                    border: 3px;
                    border-style: inset;
                }

                .card {
    position: relative;
    display: flex;
    flex-direction: column;
    min-width: 0;
    word-wrap: break-word;
    background-color: #fff;
    background-clip: border-box;
    border: 0 solid rgba(0,0,0,.125);
    border-radius: .25rem;
}

.card-body {
    flex: 1 1 auto;
    min-height: 1px;
    padding: 1rem;
    
}
.card {
		box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
		
}

            </style>